[server]
port = 8502

# Other useful server configurations (optional):
# headless = true  # Set to true if running on a server without a browser
# address = "localhost"  # Set to "0.0.0.0" to make accessible from other machines
