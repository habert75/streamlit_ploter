apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamlit-plotter-app
spec:
  selector:
    matchLabels:
      app: streamlit-plotter-app
  template:
    metadata:
      labels:
        app: streamlit-plotter-app
    spec:
      containers:
      - name: streamlit-plotter-app
        image: utibeabasi6/argocd-code:v1.0.8
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 5000

---

apiVersion: v1
kind: Service
metadata:
  name: argocd-svc
spec:
  selector:
    app: streamlit-plotter-app
  ports:
  - port: 80
    targetPort: 5000
